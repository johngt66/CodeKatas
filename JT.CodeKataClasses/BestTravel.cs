
using System;
using System.Collections.Generic;

public static class SumOfK
{
    public static int? chooseBestSum(int t, int k, List<int> ls)
    {
        return ProcessTravel(new List<int>(), t, k, ls);
    }

    private static int? ProcessTravel(List<int> included, int t, int k, List<int> ls)
    {
        included.Add
    }
}
